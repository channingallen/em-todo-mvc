<ul class="todo-container__task-list">
  {{#each tasks as |task|}}
    {{task-row task=task
               checked=(readonly task.completed)
               toggle=(action "toggleTaskStatus" task)
               remove=(action "removeTask" task)
               rename=(action "renameTask" task)}}
  {{/each}}
</ul>
